import OasisClient from '@/components/OasisClient';

export default function Home() {
  return (
    <main className="relative h-screen w-screen overflow-hidden bg-oasis-black">
      <OasisClient />
    </main>
  );
}
