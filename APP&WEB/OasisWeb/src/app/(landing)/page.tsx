import OasisClient from '@/components/OasisClient';

export default function Home() {
  return (
    <main className="relative overflow-hidden bg-oasis-black" style={{ height: '100%', width: '100%' }}>
      <OasisClient />
    </main>
  );
}
