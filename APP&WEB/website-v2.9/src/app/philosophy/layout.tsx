import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Philosophy · ZION',
  description: 'ZION philosophy — AI Native, Human Native, Universe Native. Consciousness levels, awakening stages, and the bridge between technology and spirit.',
};

export default function PhilosophyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
