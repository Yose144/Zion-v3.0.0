# ZION V3 Docker Compose — example environment file.
# Copy to .env and set ANKR_API_KEY externally (never commit the real value).
ANKR_API_KEY=<SET_VIA_SECURE_ENVIRONMENT_DO_NOT_COMMIT>
ZION_BRIDGE_CONFIG=/etc/zion/bridge-mainnet.toml
