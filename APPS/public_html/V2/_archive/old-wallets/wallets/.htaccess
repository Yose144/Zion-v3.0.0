# Zakázat přímý přístup k JSON souborům walletů
<FilesMatch "\.json$">
    Deny from all
</FilesMatch>

# Povolit přístup k PNG obrázkům (QR kódy)
<FilesMatch "\.png$">
    Allow from all
</FilesMatch>

# Zakázat výpis adresáře
Options -Indexes
