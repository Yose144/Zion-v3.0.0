[package]
name = "zion-atomic-swap"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "ZION L1 atomic-swap / HTLC daemon — SHA-256 hash-time-locked contracts for trustless cross-chain swaps"

[[bin]]
name = "zion-atomic-swap"
path = "src/main.rs"

[dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1.1"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# HTTP client (L1 RPC calls)
reqwest = { version = "0.12", features = ["json", "rustls-tls"] }

# HTTP server (swap API: /swap/claim, /swap/refund, /swap/:hash)
axum = { version = "0.7", features = ["macros"] }
tower = "0.4"
tower-http = { version = "0.5", features = ["cors", "trace"] }

# Crypto
sha2 = "0.10"          # SHA-256 for preimage verification
sha3 = "0.10"          # Keccak-256 for EVM event topic hashing (evm_watcher)
hex = "0.4"
ed25519-dalek = { version = "2", features = ["serde"] }  # Sign L1 TX from escrow key
rand = "0.8"

# Database (HTLC state persistence)
rusqlite = { version = "0.31", features = ["bundled"] }

# Error handling
thiserror = "2"
anyhow = "1"
zeroize = { version = "1.7", features = ["zeroize_derive"] }

# Time
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
tokio-test = "0.4"
tempfile = "3"
