build(deploy): update Docker/web artifacts for mainnet website deploy

- Update Dockerfile.zion-web to node:22-alpine to match the Node 22
  requirement of the website dependencies.
- Update APP&WEB/website-v2.9/.dockerignore to keep node_modules and .next
  in the build context (required because the Edge server builds the image
  from host artifacts, not inside Docker).
- Fix DAO treasury serialization in V3/L2/dao/src/api.rs and the matching
  TS type in APP&WEB/website-v2.9/src/lib/dao-api.ts so u128 balances are
  returned as strings and avoid JSON number precision issues.
- Remove leftover .commit_msg.txt.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
